# 🚀 Upload STREETMOOD para GitHub - SEM INSTALAR NADA

## Método: Upload via Navegador (100% Online)

### Passo 1: Criar Repositório no GitHub
1. Vai a: **https://github.com/new**
2. Faz login (ou cria conta se não tiveres)
3. **Repository name:** `streetmood-catalog`
4. Marca **"Public"**
5. **NÃO marques** "Add a README file"
6. Clica **"Create repository"**

### Passo 2: Upload dos Ficheiros via Navegador

Na página do repositório que acabaste de criar:

1. Clica no botão **"uploading an existing file"** (ou simplesmente arrasta ficheiros)
2. **Arrasta e solta** estes ficheiros e pastas:
   - `index.html`
   - `style.css`
   - `streetmood_images_mapping.json`
   - `all_products_output.js`
   - Pasta `scripts/` (com `main.js` e `products.js`)
   - Pasta `imagens_produtos/` (com todas as imagens)
   - Pasta `3d/` (com o README.md)

3. **OU** clica em **"Add file"** → **"Upload files"** e seleciona todos os ficheiros

4. No final da página, escreve: `Initial commit`
5. Clica **"Commit changes"**

### Passo 3: Ativar GitHub Pages

1. No teu repositório, clica em **"Settings"** (canto superior direito)
2. No menu lateral esquerdo, clica em **"Pages"**
3. Em **"Source"**, escolhe **"main"** (ou "master")
4. Clica **"Save"**
5. Espera 2-3 minutos

### Passo 4: Aceder ao Site

O teu site estará disponível em:
```
https://[TEU-USERNAME].github.io/streetmood-catalog/
```

**Exemplo:** Se o teu username é `joaosilva`, o link será:
```
https://joaosilva.github.io/streetmood-catalog/
```

---

## 📋 Checklist de Ficheiros para Upload

Certifica-te de que enviares estes ficheiros:

### Ficheiros Principais:
- ✅ `index.html`
- ✅ `style.css`
- ✅ `streetmood_images_mapping.json`
- ✅ `all_products_output.js`

### Pastas:
- ✅ `scripts/` → com `main.js` e `products.js`
- ✅ `imagens_produtos/` → com todas as imagens .jpg
- ✅ `3d/` → com `README.md` (e `puremoney.glb` se tiveres)

---

## ⚠️ IMPORTANTE

- As imagens são grandes? Podes fazer upload em batches separados
- GitHub permite até 100MB por ficheiro
- Se tiveres muitas imagens, faz upload da pasta `imagens_produtos/` de uma vez

---

## 🔄 Atualizar o Site Depois

Para atualizar o site no futuro:
1. Vai ao repositório no GitHub
2. Clica no ficheiro que queres atualizar
3. Clica no ícone de lápis (✏️) para editar
4. Faz as alterações
5. Clica **"Commit changes"**
6. O site atualiza automaticamente em 1-2 minutos

---

## 🆘 Problemas?

**Site não aparece após 5 minutos:**
- Verifica Settings → Pages está ativado
- Verifica que o ficheiro `index.html` está na raiz do repositório

**Imagens não aparecem:**
- Verifica que a pasta `imagens_produtos/` foi enviada
- Verifica que os caminhos no JSON estão corretos

**Produtos não aparecem:**
- Verifica que `all_products_output.js` foi enviado
- Verifica que `scripts/main.js` foi enviado

---

## ✨ Pronto!

Depois de fazer tudo isto, partilha o link aqui e eu ajudo a verificar se está tudo a funcionar! 🚀

